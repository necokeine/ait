[package]
name = "ait-workspace"
version.workspace = true
edition.workspace = true
publish.workspace = true

[features]
contract-tests = []

[dependencies]
ait-domain = { path = "../domain" }
async-trait = "0.1"

[lints]
workspace = true
