//! Paseo project projections, separate from persisted registry records and lease RPCs.

pub use crate::protocol::workspace::{
    ProjectKind, ProjectPlacementPayload, WorkspaceProjectDescriptorPayload,
};
