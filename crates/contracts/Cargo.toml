[package]
name = "ait-contracts"
version.workspace = true
edition.workspace = true
publish.workspace = true

[features]
dev-mock-provider = ["ait-domain/dev-mock-provider"]

[dependencies]
ait-domain = { path = "../domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
