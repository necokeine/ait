{
  "name": "ait-paseo-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/app",
    "apps/paseo",
    "packages/protocol",
    "packages/client",
    "packages/relay",
    "packages/highlight",
    "packages/plugin",
    "packages/expo-two-way-audio"
  ],
  "scripts": {
    "dev:paseo": "npm run dev --workspace=@getpaseo/desktop",
    "build:server": "cargo build -p server-bin --bin server",
    "build:app-deps": "npm run build:sdk && npm run build:highlight && npm run build:plugin && npm run build --workspace=@getpaseo/expo-two-way-audio",
    "build:paseo": "npm run build:app-deps && npm run build:main --workspace=@getpaseo/desktop",
    "postinstall": "node scripts/postinstall-patches.mjs",
    "build:relay": "npm run build --workspace=@getpaseo/relay",
    "build:sdk": "npm run build --workspace=@getpaseo/client",
    "build:client": "npm run build:sdk",
    "test:sdk": "npm run build:sdk && npm run test --workspace=@getpaseo/protocol --workspace=@getpaseo/client --workspace=@getpaseo/relay -- --exclude src/e2e.test.ts --exclude src/live-relay.e2e.test.ts",
    "build:protocol": "npm run build --workspace=@getpaseo/protocol",
    "build:highlight": "npm run build --workspace=@getpaseo/highlight",
    "build:plugin": "npm run build --workspace=@getpaseo/plugin",
    "watch:protocol": "npm run watch --workspace=@getpaseo/protocol",
    "watch:client": "tsc -p packages/client/tsconfig.json --watch --preserveWatchOutput",
    "build:desktop": "npm run build:desktop-assets && npm run build --workspace=@getpaseo/desktop",
    "dev:app": "node apps/app/scripts/dev-server.mjs",
    "build:desktop-assets": "npm run build:app-deps && npm exec --workspace=@getpaseo/app -- cross-env PASEO_WEB_PLATFORM=electron expo export --platform web",
    "build:dmg": "npm run build:dmg --workspace=@getpaseo/desktop",
    "build:dmg:release": "npm run build:dmg:release --workspace=@getpaseo/desktop",
    "build:ios": "npm run build:ios --workspace=@getpaseo/app",
    "build:ios:simulator": "npm run build:ios:simulator --workspace=@getpaseo/app",
    "build:ios:ipa": "npm run build:ios:ipa --workspace=@getpaseo/app"
  },
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20260423.1",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "get-port-cli": "^3.0.0",
    "oxfmt": "0.46.0",
    "oxlint": "1.61.0",
    "patch-package": "^8.0.1",
    "playwright": "^1.56.1",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "@codemirror/language": "6.12.4",
    "@codemirror/view": "6.43.6",
    "lightningcss": "1.30.1",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-native-reanimated": "4.3.1",
    "react-native-worklets": "0.8.3"
  }
}
